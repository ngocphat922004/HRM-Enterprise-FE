export interface EditPositionForm {
    maCV: number;
    tenCV: string;
    moTa: string;
    heSoPhuCap: number | null;
}

export interface SidebarItem {
    label: string;
    icon: string;
    route: string;
}