export interface AddPositionForm {
    tenCV: string;
    moTa: string;
    heSoPhuCap: number | null;
}

export interface SidebarItem {
    label: string;
    icon: string;
    route: string;
}